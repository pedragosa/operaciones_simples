#ifndef __LLIBRERIA_PROPIA_H__
#define __LLIBRERIA_PROPIA_H__

#define SUM '+'
#define RES '-'
#define MUL '*'
#define DIV '/'
#define MAX '>'
#define MIN '<'

float suma(float,float);
float resta(float,float);
float multiplica(float,float);
float divideix(float,float);
float maxim(float,float);

void mostrarResultat(float,float,char);

#endif